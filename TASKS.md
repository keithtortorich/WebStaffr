# TASKS.md — WebStaffr

## Purpose
Single source of next actions. Sorted by smallest atomic step. No future speculation entries — only what's actually next.

## Execution Model
Claude operates this queue via a single loop: Verify -> Decide -> Execute -> Verify. See CLAUDE.md's "Execution Model" section for the full rule, including which changes may be self-approved versus which require explicit approval.

## Current Queue
Owner noted per item per the Claude/Grok coordination protocol (see DECISIONS.md). Items marked (Grok) are an explicit invitation to extend a specific existing Claude-owned file per that protocol's "unless explicitly invited" exception -- not a blanket reopening of ownership. Grouped by priority tier, not just a flat list, so the next session can start at the top without re-deriving order from chat history.

### Coordination Plan (Grok + Claude)
Refined 2026-07-04 after this session's ownership-boundary friction. Adds process on top of the priority queue below; doesn't replace it -- the queue keeps the specific rationale for each item (why P0-2 is a data-model decision, why P2 is actually blocked, etc.), which a compressed rules-only doc would lose.

- **Parallel work split**: Claude -- site generator, widget wiring, Docker, `angel.py` integration, frontend, governance/docs. Grok -- resilience and test coverage *strictly within* `voice.py`, `ghl.py`, or genuinely new files (not a general "backend resilience" mandate that could read as covering `angel.py` again); credential/secrets pattern docs; Docker verification as proposals only, never direct edits.
- **Review & Merge Gate**: two accepted paths, same gate either way -- full test run (`unittest discover -s tests` + `health_check.py`) before anything lands on `main`. (a) Paste: founder pastes Grok's proposal, Claude applies/tests/commits. (b) PR: Grok forks the public repo and opens a real GitHub PR (no write access to this repo needed -- only to Grok's own fork); founder relays the PR link/number instead of the diff; Claude fetches it directly (`git fetch origin pull/<N>/head`), reviews and tests it against the real local clone, and merges if clean. Path (b) is untested as of this writing -- first real attempt will confirm whether Grok's environment can actually execute fork+PR creation.
- **Efficiency Rules for Grok**: read current state directly from the public repo (`TASKS.md`, `git log`, relevant files) before proposing anything -- don't rely on the founder narrating status. Deliver complete, self-tested proposals (full file + relevant tests) per task rather than incremental back-and-forth. Paste remains the default; PRs are the path when convenient (see Review & Merge Gate above).
- **Sync & handoff**: end-of-session updates to this file and `DECISIONS.md` stay single-narrated (see Process notes below) even when both assistants contributed content to a session -- avoids two independent writers drifting on the same ledger.

**Before starting anything below**: run `./.venv/bin/python -m unittest discover -s tests` and `./.venv/bin/python scripts/health_check.py`. Both should be green (last confirmed: 54/54 tests, HEALTHY, commit `cd5020d`). If either isn't, that's the actual first task, regardless of what's listed here.

### P0 -- Founder-only, blocks everything below it
1. [ ] Supply real `GHL_API_KEY` / `GHL_LOCATION_ID` and/or `GROK_API_KEY` as env vars, never committed. Both `GrokVoiceBackend` and `GoHighLevelClient` are fully implemented and tested against mocks (see `CREDENTIALS.md`) but `[Unverified]` against live accounts -- nothing below can close that out without these.
2. [ ] Decide the scope of item P1-1 below (appointment status/cancelled data model). This is a data-model change (new migration) -- outside self-approval per CLAUDE.md's Decision Framework, needs an explicit yes/no/how before Claude builds it.
3. [ ] Locate or write an AOKAI spec, or explicitly declare it out of scope. It's been referenced since the original integration request and has never had any spec anywhere in Drive or the Angel Package doc -- neither assistant can scope real work against it as-is.

### P1 -- Claude, ready now (pending P0-2's decision)
1. [ ] Wire `GoHighLevelClient.update_appointment`/`cancel_appointment` into `Angel`/the local appointment flow, once P0-2 is answered. Needs a new migration adding a status/cancelled column to `appointments` (currently only has `ghl_synced`) so local state can't drift from GHL's.

### P2 -- Claude, blocked on something other than a founder decision
1. [ ] Wire the widget (`angel-widget.js`) onto an actual generated customer site. Blocked because no generated site exists yet -- and more broadly, the site-generator/frontend work that was the *other* half of the original "WebStaffr Integration" scope (and nominally Claude's declared domain in the Grok/Claude split) hasn't been started at all this session; everything built so far is Angel/backend. Worth the founder flagging explicitly whether that's still wanted next, or whether Angel is the whole product for now.

### P3 -- Grok, within already-invited scope
1. [ ] Task 3: verify `Dockerfile`/`docker-compose.yml` actually build and run, if Grok's environment has Docker (this one doesn't). Verify and report only -- those files are Claude-owned; if the build surfaces a bug, don't edit directly.
2. [ ] Once P0-1 lands: live-test `GrokVoiceBackend` against a real xAI account and `GoHighLevelClient`/`update_appointment`/`cancel_appointment` against a real GHL account, report results back so the `[Unverified]` labels in `CREDENTIALS.md`/`DECISIONS.md` can be resolved one way or the other.

### Process notes for whoever picks this up next
- Check `TASKS.md`'s Completed log (and `git log --oneline`) against any proposed "next step" before starting it -- three separate times this session, a proposed task turned out to already be done. Costs nothing to check, costs a full round-trip not to.
- Escape-artifact check before every commit: search each of `CLAUDE.md`, `TASKS.md`, `DECISIONS.md`, `README.md`, `CREDENTIALS.md` for a stray backslash immediately followed by a backtick (an artifact from an earlier over-escaped heredoc write, fixed once already this session) -- count should be 0 for all five.
- Run the full test suite + health check before *and* after every change, not just at the end of a session.
- Ownership map as it stands: Grok-extended (invited) -- `webstaffr/workers/angel/voice.py`, `webstaffr/workers/angel/ghl.py`. Claude-owned, not open to edits without a fresh explicit invitation -- `angel.py`, `db.py`, `repository.py`, `migrations/*.sql`, `router.py`, `booking.py`, `Dockerfile`, `docker-compose.yml`, and all governance docs (this file, `DECISIONS.md`, `CLAUDE.md`, `ARCHITECTURE.md`, `PROJECT.md`).

## Completed
- Added bounded retry to `Angel`'s GHL sync calls (`webstaffr/workers/angel/angel.py`): a new `Angel._call_ghl_with_retry()` helper (`ghl_max_attempts` constructor param, default 3, validated `>= 1`) wraps `create_appointment` in `book_appointment()` and `log_note` in `log_note_to_ghl()`, same bounded-attempts-no-sleep shape as `WorkflowExecutor._run_step`. Each failed attempt is logged; the existing outer try/except (booking succeeds locally regardless of GHL outcome; `log_note_to_ghl` returns True/False, never raises) is unchanged -- this only adds retry underneath it. Added `tests/test_angel.py` coverage: two new tests confirm a client that fails N times then succeeds still ends up synced/`True` after retrying; two existing always-failing-client tests now also assert the exact call count (3) to confirm retries actually happen, not just a single attempt; one new test confirms `ghl_max_attempts` is configurable and validated (rejects `0`). Per CLAUDE.md's chaos-engineering practice. 54/54 tests passing, health check HEALTHY (2026-07-04).
- Grok added `update_appointment`/`cancel_appointment` to the `GHLClient` Protocol, `NullGHLClient` (with recording lists matching the existing pattern), and `GoHighLevelClient` (reusing `_request()`, real endpoints marked `[Unverified]`) in `webstaffr/workers/angel/ghl.py`, relayed via the founder and merged as-is -- no integration fixes needed this round (no print()/broad-except issues like the voice.py round). Added `tests/test_angel.py` coverage: `NullGHLClient` recording for both new methods, and two tests verifying `GoHighLevelClient.update_appointment`/`cancel_appointment` call `_request()` with the expected method/path (mocked, no live GHL account). File ownership comment updated to `## OWNED_BY: GROK (extended from Claude base)`. Did NOT wire this into `angel.py` or local appointment state -- see queue item 8, that needs a data-model decision first. 51/51 tests passing, health check HEALTHY (2026-07-04).
- Grok implemented the real `GrokVoiceBackend.respond()` call against xAI's chat-completions endpoint (using `context["system_prompt"]` as instructed, credential check preserved), relayed via the founder and integrated into `webstaffr/workers/angel/voice.py`. Fixes applied during integration: replaced `print()` with `logger.warning()` (consistent with the rest of the codebase); narrowed the single blanket `except Exception` into `except httpx.HTTPError` (transport/API failures) and `except (KeyError, IndexError, ValueError)` (unexpected response shape) with distinct log messages, so a real bug in response parsing isn't indistinguishable in logs from an ordinary network blip; corrected the "realtime API" framing in comments/docstring to accurately describe a standard synchronous chat-completions call (not the WebSocket/audio realtime API); flagged the model name and endpoint as `[Unverified]` in the docstring since there's no live xAI account to test against here. Grok's reply described a test update but didn't include it, so wrote the replacement tests myself: `tests/test_angel.py::TestVoiceBackends` now covers a successful mocked API call, a transport-failure degrade path, and a malformed-response degrade path (via a small `_FakeHTTPResponse` stand-in -- no real network calls, consistent with the rest of this suite). File ownership comment updated to `## OWNED_BY: GROK (extended from Claude base)`. 49/49 tests passing, health check HEALTHY (2026-07-04).
- Added a `POST /book` endpoint to `webstaffr/workers/angel/router.py`, exposing `Angel.book_appointment` over HTTP for callers that don't go through a `/chat` or `/webhooks/ghl` conversation turn (module-level `BookAppointmentRequest`/`BookAppointmentResponse`, same 400-before-DB validation pattern as the other endpoints). While testing it, found a real latent bug: `create_app()`'s migration only ran inside the ASGI `lifespan` handler, and FastAPI's `TestClient` doesn't reliably fire lifespan events unless entered as a context manager (`with TestClient(app) as client:`) -- `tests/test_router.py` and `scripts/health_check.py` were both using the bare `TestClient(app)` form. `/chat` and `/webhooks/ghl` never touched a table so this was masked until `/book` hit "no such table: tenants" against a real temp-file DB. First attempted fix (running `migrate()` eagerly inside `create_app()` itself) was caught and reverted before commit -- it made merely *importing* `router.py` create/migrate a real `webstaffr.db` file in the cwd as a side effect (the module builds a default `app` instance at import time), which briefly got staged into git. Corrected fix: switched both `tests/test_router.py`'s `RouterTestCase` and `health_check.py`'s router smoke check to enter `TestClient` as a context manager, which is the correct way to trigger lifespan startup; `create_app()` itself has no import-time side effects again. Added `*.db`/`*.sqlite*` to `.gitignore` as defense in depth. Added `tests/test_router.py::TestBookEndpoint` (5 tests) and extended the router smoke test to hit `/book`. 47/47 tests passing, health check HEALTHY (2026-07-04).
- Founder updated the Angel Package Drive doc with the real core prompt (previously referenced as "provided earlier" but missing). Re-synced `docs/drive-mirrors/Angel_Package.md`, replaced the draft placeholder in `webstaffr/workers/angel/angel_prompt.md` with the real text, reworked `Angel.render_prompt()` to append dynamic context as a structured block (the real prompt has no `{business_name}`-style template placeholders), and wired the rendered prompt into `respond()` via `context["system_prompt"]` so it's actually used, not dead code. Updated tests accordingly (42/42 passing); health check HEALTHY (2026-07-04).
- Governance baseline written and committed: CLAUDE.md, PROJECT.md, ARCHITECTURE.md, DECISIONS.md (2026-07-04).
- First system behavior decided: internal workflow execution (2026-07-04).
- Minimal design recorded: tenant_context, workflow_definition, workflow_executor, execution_record (2026-07-04).
- Single-loop execution model adopted (2026-07-04) -- see CLAUDE.md, DECISIONS.md.
- Resolved local/remote git divergence; merged and pushed governance baseline to `github.com/keithtortorich/WebStaffr` at `fc4dc46` (2026-07-04).
- Diagnosed and cleared repeated `.git` lock-file corruption; standardized on Desktop Commander for all git operations (2026-07-04).
- Removed stale local `master` branch (2026-07-04).
- Adopted the autonomous Engineering Director operating model in CLAUDE.md (2026-07-04) -- see DECISIONS.md.
- Resolved the Phase 1 scope conflict: minimal-first, infra added only when a specific feature needs it (2026-07-04) -- see DECISIONS.md.
- Implemented the first slice: `webstaffr/tenant.py`, `workflow.py`, `execution.py`, `executor.py` (2026-07-04).
- Added `tests/test_executor.py` (10 tests) and `scripts/health_check.py` (2026-07-04).
- Recorded model-usage guideline in CLAUDE.md: Sonnet 5 default, Opus for high-stakes work only (2026-07-04).
- Added SQLite persistence: `webstaffr/db.py`, `webstaffr/migrations/0001_initial.sql`, `webstaffr/repository.py`. 21/21 tests passing (2026-07-04) -- see DECISIONS.md.
- Fixed a stray backslash-backtick escaping artifact in CLAUDE.md (2026-07-04).
- Investigated Google Drive as source of truth for a larger integration request; found significant version sprawl (5+ Business Plan copies, 4 Financial Model copies) and no Angel/AOKAI spec docs at the time; escalated via clarifying questions rather than guessing (2026-07-04).
- Founder supplied "Angel Package" spec doc in Drive; mirrored locally (labeled, non-authoritative copy) at `docs/drive-mirrors/Angel_Package.md` (2026-07-04).
- Recorded scope-expansion decision: Angel worker, FastAPI, GoHighLevel, Grok voice, per founder's own spec (2026-07-04) -- see DECISIONS.md.
- Built Angel worker: `webstaffr/workers/angel/{angel.py, voice.py, ghl.py, booking.py, angel_prompt.md}`. Voice (Grok) and GHL clients are credential-checked interfaces with safe Null defaults for testing/offline use -- neither is a working live integration yet (no credentials available to build/test against). Draft placeholder prompt used since the founder's real prompt wasn't found in Drive (2026-07-04).
- Added `webstaffr/migrations/0002_angel_appointments.sql` (tenant-scoped appointments table) (2026-07-04).
- Built FastAPI router (`webstaffr/workers/angel/router.py`): `/health`, `/chat`, `/webhooks/ghl` (website_lead, missed_call). Added `fastapi`, `uvicorn`, `httpx` as new pinned dependencies (`requirements.txt`), first dependencies this repo has ever had (2026-07-04) -- see DECISIONS.md.
- Built `webstaffr/workers/angel/widget/angel-widget.js` -- vanilla JS embeddable chat widget; voice button present but explicitly informs the visitor voice isn't available yet, rather than silently doing nothing (2026-07-04).
- Added `Dockerfile`, `docker-compose.yml`, `.dockerignore` for local testing. Not build-verified (no Docker on this machine) -- instead verified the same code path directly via `uvicorn`, hitting `/health`, `/chat`, `/webhooks/ghl` over real HTTP with a real file-based SQLite DB (2026-07-04).
- Added `tests/test_angel.py` (13 tests) and `tests/test_router.py` (7 tests). Total 40/40 tests passing. Extended `scripts/health_check.py` with `angel_imports`, `angel_booking_round_trip`, `angel_router_smoke` checks -- HEALTHY (2026-07-04).
- Updated README.md with local dev setup instructions (2026-07-04).
- Corrected a factual claim from a second AI assistant ("Grok," run separately by the founder) that "Grok Voice" was live -- verified against `voice.py` that `GrokVoiceBackend.respond()` still raises `NotImplementedError` by design. Founder confirmed the correction and proposed a founder-mediated Claude/Grok coordination protocol (file-ownership comments, DECISIONS.md as shared log); adopted, with the ownership split's overlap against already-built backend files flagged back to the founder rather than assumed (2026-07-04) -- see DECISIONS.md.
- Founder resolved the ownership-overlap flag: file ownership applies prospectively only, not retroactively. Tagged existing backend-core files with `## OWNED_BY: CLAUDE` (or the language-appropriate comment syntax): `db.py`, `repository.py`, both migration files, all of `workers/angel/*.py`, `angel-widget.js`, `Dockerfile`, `docker-compose.yml`. Deliberately did not tag `angel_prompt.md` (read verbatim as Angel's live system prompt) or `docs/drive-mirrors/Angel_Package.md` (has its own Drive-mirror header). 42/42 tests still passing, health check HEALTHY (2026-07-04) -- see DECISIONS.md.
